import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  getCreditContainer: {
    flex: 1,
    backgroundColor: '#000',
  },
  getCreditContent: {
    marginBottom: 50,
    padding: 16,
  },
  scrollViewContent: {
    height: '100%',
  },
});

export default styles;
