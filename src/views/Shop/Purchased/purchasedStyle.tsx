import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  purchaseContainer: {
    flex: 1,
    backgroundColor: '#000',
  },
  purchaseContent: {
    marginBottom: 50,
    padding: 16,
  },
  scrollViewContent: {
    height: '100%',
  },
});

export default styles;
